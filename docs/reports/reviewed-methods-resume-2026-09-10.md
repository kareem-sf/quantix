# Reviewed methods and foundation repairs — 10 September 2026

Quantix now has a reviewed method library connected to the actual Tender Manager and staff execution path. This continuation preserves the other agent's working source and corrects the earlier claim that T001–T013 were all complete. The [single master plan](../superpowers/plans/2026-09-09-adaptive-office-master.md) retains every outstanding task and acceptance criterion.

## What works in this increment

| Engineer action or execution boundary | Implemented behavior |
| --- | --- |
| Settings → Methods | Inspect included or selected local packages, review exact files/hashes/requirements, activate the persisted snapshot, retain history and disable/re-enable specific versions. |
| Company and Tender availability | A Tender override, including disable, takes precedence only in that Tender. Disabling an inherited company method creates a real Tender override and leaves company availability intact. |
| Work review | Select exact installed versions alongside their required tools. Installation selects nothing by default and grants no source, route, provider, budget or tool authority. |
| Manager and staff execution | Both actual direct and original-client MCP tool bridges support scoped description discovery, exact instruction loading and bounded declared reference reads. Each actor loads only approved versions inside its own active assignment/root. |
| Applicability and publication | Restricted methods use saved Tender jurisdiction/edition. Changed bytes, disabled packages or changed applicability prevent stale output publication, including summary-only output. |
| Staff desk | Shows real instruction/reference-use receipts. Exact package/member/delivered hashes, character ranges, profile and assignment identity remain expandable. Open desks refresh after office work changes. |
| Included content | Two authored 1.1.0 packages: received-package triage and quantity reconciliation. Neutral applicability; no invented country/edition restriction. |
| Backups | Real immutable method archives are included and verified with exact snapshot hashes, measured sizes and strict archive membership. Restore staging retains those archives. |

Packages use strict `quantix-skill.json` manifests and `SKILL.md`. Original folders are preserved; snapshots and immutable objects live under the managed home. Inspection checks declared file hashes/sizes, safe paths, required tool versions and work-review identity eligibility. Reparse points, links, traversal and ambiguous members are rejected. Same-version changed bytes conflict. Old unvalidated prototype records remain history and cannot execute.

Script execution is explicitly unavailable. This increment does not treat a demonstration subprocess as a Python sandbox or claim that all 39 planned method families and the full plugin catalogue exist.

## Repairs found while checking the previous progress

- Staff lifecycle/preferences now append immutable versions. Historical work orders and replay receipts preserve their exact original version. Retired/archived colleagues cannot admit fresh bindings or assignments.
- Notebook writes and prompt reconstruction validate actor, assignment, active root, source scope and current saved-result bases. They do not grant general cross-assignment memory or unrestricted handoff reads.
- Read-only tool output may contain legitimate saved fields such as `decision`. Invalid JSON/nonfinite numbers/oversized output still fail validation. Source inspections and events commit only after successful result validation, including Manager reads.
- Arithmetic review recomputes supported persisted calculations and records the exact method and basis. Words such as “area” and arbitrary numbers in prose no longer manufacture findings. Unsupported calculations remain incomplete.
- PDF reprocessing no longer nests the same PDFium lock during OCR rendering. It releases page handles before rendering, drains subprocess output, observes timeout/cancellation and reports blank/failed OCR coverage honestly. The public endpoint requires an exact original hash belonging to the selected Tender and bounds page limits; actual original bytes are rechecked.
- Generic acceptance examples now use real package activation measurements, exact jurisdiction rejection, a real PDF and saved arithmetic. Their passing result is evidence for those examples, not completion of their entire task descriptions.

## Verification

The source was captured from the user's current uncommitted project (847 files) into an isolated worktree. Luna/xhigh implementation assignments and Astra/xhigh architecture/quality reviews used at most three concurrent children. Review corrections were reproduced and tested before the final scoped verdicts passed.

| Gate | Result |
| --- | --- |
| Full backend suite | 819 passed, one existing platform-dependent skip; eight dependency deprecation warnings; 459.87 seconds. Includes all 95 generic agentic examples. |
| Full UI suite | 233 passed across 50 files. After the final recovery/copy corrections, 21 affected UI tests passed, including all 10 Method Library cases; typecheck passed again. |
| API and launcher | Generated frontend bindings refreshed, frontend typecheck passed, five source-workspace launcher tests passed. Workspace and office compatibility revisions are now 2. |
| Style | Ruff passed for changed Python files; Prettier passed for changed frontend/launcher files. A repository-wide whitespace check still reports a pre-existing blank EOF in untouched `office_research.py`; affected tracked-file checks pass. |
| Independent review | Runtime/staff repair and exact reviewed-method integration both passed their bounded final rereviews. |

The original inherited full backend run stalled in a nested PDFium lock. A separate diagnostic run identified that deadlock; both were stopped without claiming a pass. An intermediate combined npm invocation completed 230 UI checks but rejected an extra CLI flag before backend execution; the final separate commands above supersede that invocation.

Real-app verification used a separate synthetic home beneath `~/.quantix/tmp`, the actual FastAPI app and React UI, and controlled provider adapters. The completed Manager → generated colleague → Manager chain used actual persisted sources, assignments, tool bridges and method receipts. No paid provider call was required.

The browser walkthrough exercised package inspection/activation, exact unselected-to-selected work-review methods, saving choices without starting work, company/Tender disable scope, staff method history, expandable provenance, source preview and return, and retained unsent Manager text. Light and dark appearance were inspected at the normal 1280×720 viewport and compact 1024×768 / 768×900 layouts. Temporary viewport overrides were cleared afterward. Native Windows tray interaction, true OS DPI changes, connected tablet and microphone use were not exercised.

Transient local browser fetch failures appeared during initial loads. Method-list retry and source-viewer reopening recovered the affected reads; the Methods recovery action covers failed library, included-package and Tender-list loads without discarding a folder draft. These observations are not represented as a clean zero-error browser run.

## Remaining work

Actual runtime depth/concurrency remains 1/1. Complete adaptive work-graph execution, full result handoff consumption, checkpoint continuation, general continuing notebooks and independent review rooms remain open. Expanded RAG/currentness and research, the full method/plugin/MCP lifecycle and a validated isolated Python runner remain in the master plan. The reader repair alone does not wire extraction derivatives through complete RAG.

BIM/IFC, Ollama and the excluded section 11 business connectors remain outside this work. Connected tablet and voice retain their later-delivery status. No release package, commit, push, real Tender approval, commercial send, factory reset or mutation of the normal populated home was performed.

## Source integration

The verified continuation was copied back to `D:\AI Work\quantix` on its existing `rebuild` branch without committing. A hash audit confirmed all 80 changed/new/deleted source paths and preserved the other 792 captured files byte-for-byte. Five integrated-project checks passed for the actual HTTP method library, both Manager/staff tool bridges and method backup/restore staging. The synthetic preview services were stopped; the normal app/service was not restarted. If an older service is still running, finish or stop its active work, choose Quit, then reopen Quantix to load revision 2. The original source capture and final review/integration records remain in the ignored local execution ledger.

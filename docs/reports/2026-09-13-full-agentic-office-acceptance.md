# Full agentic office — integration acceptance, 13 September 2026

This is an execution record for the approved desktop/text increment. It distinguishes service tests, actual local execution, browser observations and unperformed live/platform checks. The implemented workflows pass the local regression gates below. The whole programme remains open for the listed live and native-platform acceptance.

## Regression gates

The first full backend run (`npm run test:backend -- --disable-warnings`) completed in 711 seconds: **1,013 passed, 10 failed, one skipped**. Failures covered old profile-to-draft projections, implicit work-product versioning assumptions, old delegation defaults, lightweight classification, original-client protocol fixtures and streaming request-budget fixtures. They were resolved without weakening authority checks. A subsequent full integration run passed **1,071 tests with one skip**.

After the final code/memory inspectors, native-call limits and runtime-proof changes, the full backend gate passed **1,096 tests with one existing platform skip** in 769.81 seconds. The full UI gate initially passed 329 tests; after the final read-error truthfulness fix, it passed **330 tests in 75 files** in 202.45 seconds. Logs are retained under `~/.quantix/cache/development/2026-09-12-full-agentic-office/final-backend.txt`, `final-ui.txt` and `final-ui-2.txt`. Generated OpenAPI/TypeScript declarations and the final frontend typecheck passed. Four actual Node filesystem-watcher tests and launcher syntax checks passed separately.

The first full UI discovery run had 314 passes and one then-unfinished memory revision-refresh test. The corrected test is in the final passing gate. An early duplicate-key warning in ReviewRoom came from its fixture assigning the same ID to two added findings; the fixture now creates distinct IDs. A separate SubmissionRequirements sibling-key collision was corrected with distinct component keys. Remaining jsdom CSS-parser iteration notices are test-environment warnings, not hidden browser failures.

Root-focused checks include cancellation/reset admission regressions, unsupported-rounding validation, mixed mass scales, emission-factor units, metric/imperial conversion and invalid-operation checks. Independent review also exposed low-digit loss at ambient Decimal precision. The final calculator uses explicit 1,024-digit arithmetic precision and exact rational Pint unit factors, with bounded named-unit expressions. Two meaningful red tests reproduced the precision loss before repair; 24 focused calculation/worker/review checks passed afterward. Formula engine version 3 distinguishes prior arithmetic receipts. No numerical unit conversion is delegated to model arithmetic.

## Actual local execution

- Monty 0.0.23 executed an approved declared script that called the actual calculation tool twice and combined its structured outputs, 42 + 5 = 47. Replay did not duplicate calculations. Standalone approved composition passed; unapproved Python was blocked. The C/D owner recorded 20 focused callback, revocation, source-read rollback and process-lifecycle checks.
- An additional actual engineer component check ran `6 * 7` with empty inputs, no tools, no AI/account use and no Tender-file access. It returned **42** in 0.078 seconds. Work > Activity > Run details > Local calculations and code displayed the recorded engineer identity, code, runtime hash, input hash, limits and actual output. This is a local component check, not an agent grant or live model acceptance.
- The private Podman 6.1.1 executable was installed under `~/.quantix` and its official download hash verified. WSL2 is absent. No local full-Python container, local MCP container or rendered-browser isolation qualification has run on this computer.
- Twenty-four real calculation probes and the immutable synthetic benchmark fixtures were exercised without AI requests. The benchmark driver uses normal import, scope review and jobs. Its model tests use an explicitly synthetic model, not a live provider. The strengthened evaluator requires exact calculation IDs, evidence and a distinct completed reviewer assignment; self-checks cannot qualify.
- Real multilingual embeddings used `intfloat/multilingual-e5-small` at commit `614241f622f53c4eeff9890bdc4f31cfecc418b3`, with downloaded file hashes checked. On six synthetic bilingual queries over 12 evidence records: recall@1 0.50, recall@3 1.00, MRR 0.722222; warm query times 19.24–24.18 ms. A source revision invalidated the old index; reindexing excluded the old source and returned the revised version. This small warm-cache result is not a large-Tender quality or cold-start claim.

## Browser journeys observed

Environment: the actual development UI and local authenticated backend at `http://127.0.0.1:1420`, controlled through Codex's in-app browser. Views were checked at 1440 × 1000 and 600 × 900, plus its initial 736-pixel-wide viewport. Both light and dark themes rendered. Screenshots were inspected through the browser tool. Hot-reload hook-signature changes produced development errors during implementation; a fresh reload begins the final browser check. Transient development-service reload errors were kept visible and used to verify read retries; failed reads now cannot masquerade as an empty draft/table. They do not qualify as passing journeys until the actual saved content is visible.

All acceptance writes used a newly created **Synthetic agentic acceptance — 13 September** Tender or an explicitly named synthetic library profile. No real Tender approval, provider request, commercial send or account authentication change was performed.

| Journey | Observed result |
| --- | --- |
| Manual reusable Quantity Surveyor | Saved version 1, edited working instructions into version 2, and opened both history entries. The profile remains separate from account/source permissions. |
| Profile form validation | Found required scope fields hidden under More options. The corrected form was rechecked in the real browser: it displayed Complete Specialisms, opened the required section and focused that field. The incomplete test form was closed without saving. |
| Runtime setup | Calculations and code correctly distinguished available tool composition from unavailable Python and explained the WSL2 prerequisite. |
| BOQ import | Imported two immutable synthetic DOCX originals through the normal import screen. No AI account was selected for the synthetic Tender. |
| Deterministic result and draft versions | The real API recorded 50 m² × 0.20 m = 10.00 m³ and saved comparison versions 1 and 2. Reviews displayed the 12 m³ BOQ value and −2.00 m³ difference. |
| Calculation inspector | A revised synthetic calculation recorded 50 m² × 0.30 m = 15.00 m³ and an explicit no-waste-factor assumption. Draft version 3 showed the +3.00 m³ difference and opened the exact typed inputs, units, output, assumption, rounding and formula/basis hashes. |
| Source inspection and return | Opened the exact saved paragraph from version 1 and returned to the same selected draft version. Original source bytes and saved conclusions remained unchanged. |
| Source revision | Imported a separate synthetic copy with thickness 0.30 m. The old comparison immediately displayed Needs review / source revision changed, retaining its previous 10.00 m³ result. |
| Public retrieval and citation | Retrieved `https://example.com` through the real public-fetch service, then cited its exact saved passage with an explicit synthetic-only purpose. Fetching alone remained Not cited; citation changed the state to Cited. No engineering or price claim was based on this page. |
| Working memory | Displayed a separately stored, source-linked assumption. The real final browser view showed Needs review, identified the changed source, opened its exact earlier version/hash and refused promotion. The complete UI exposes validity, reasons, dependencies, older notes/receipts and explicit engineer promotion; positive promotion and exact retry are covered by backend/UI tests. Failed reads retain previous data and never claim an empty notebook. |
| Keyboard/narrow layout | Ctrl+Alt+R/S/W navigated Reviews/Research/workspace visibility. At 600 px the workspace used the full width and preserved readable table/source/research controls. |

The page reader's system-Podman integration defect was repaired. It now uses the shared private runtime, a networkless browser and a separate constrained Unix-socket gateway that pins scheme, host, public IP and port. Setup requires real Chromium/denial probes and an allowed certificate-verified public GET, pins the image/source identities and revalidates the proof/fingerprint. Unit and proxy-policy tests pass; these checks do not substitute for the still-unrun VM qualification on this computer.

Final browser verification also exposed development reloads caused by source notifications with unchanged content. The launcher now debounces and compares actual Python-source hashes, ignoring metadata-only, identical-save and cache notifications, while detecting changed/new/deleted modules. Four real filesystem tests passed. The idle development launcher was restarted with no pending restore and no active Tender work; no further backend reload was observed during the subsequent checks. This launcher-only correction changes no Python service or Tender authority.

## Independent review and remaining acceptance

Independent static review revisited eight C/D/engineering findings: remote effects/billing, uncertain retries, reflected credentials, descendant process cleanup, maintenance leases, extension schemas, declared callbacks and missing unit-addition outputs. Follow-up found them addressed. Additional review defects were also repaired: stale work-product pagination, Decimal precision loss, browser origin/positive-path/manifest qualification, scoped notes and cancellation draining, and verified benchmark-report integrity.

Completed staff results now write checkpoints in their completion transaction using a trusted input snapshot captured before packet construction. Resume checks source, instruction, profile, messages/notebook, permissions, bundled tool/helper/dependency bytes and checked AI-software version before reusing the immutable result. It does not copy source-read receipts or repeat assignments/provider calls/spending. Known critical benchmark failures block ordinary Manager/classifier/staff admission. Explicit synthetic benchmark exceptions retain normal source/account/budget authority; synthetic executors and uploaded/self-reported success cannot clear critical errors. Exact report viewing/review revalidates immutable hashes, and unavailable metrics are never converted to zero.

Some native combinations remain intentionally unavailable: Google/xAI native search lacks an established enforceable per-request call bound; Google URL Context ignores the portable domain/content/call controls; Anthropic combined native search/fetch cannot enforce the requested shared total. The UI explains these restrictions and keeps the shared Quantix URL reader and explicitly reviewed bounded research routes separate. OpenAI enforces the lower shared search/native-call ceiling. No silent tool-setting discard, budget expansion or paid API fallback is introduced.

Live API/Codex/Grok engineering journeys and 72-attempt benchmark configurations remain open pending an explicitly selected account/model and total allowance. Hosted OpenAI wire tests used the actual SDK over HTTPX MockTransport; they are not real inference, billing or retention observations. Windows WSL2/full-container qualification and macOS/Linux machine qualification remain open. Native desktop tray/Quit/reset journeys and actual 100%/125%/150% OS scaling are not replaced by browser viewport checks. The in-app browser did not expose a working zoom control in this session, so enlarged scaling remains unverified.

No release package or commit was produced. Existing workspace changes and customer originals were preserved.
